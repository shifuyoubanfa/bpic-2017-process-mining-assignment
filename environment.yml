name: process-mining
channels:
  - defaults
dependencies:
  - python
  - graphviz
  - pip
  - pip:
      - pandas
      - numpy
      - matplotlib
      - pm4py
      - scikit-learn
      - jupyter
      - ipykernel
